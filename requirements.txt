# Device automation. The MenuTree tools talk to the phone through
# uiautomator2, falling back to plain adb where the agent will not run.
uiautomator2==3.2.5

# The deliverable: the MenuTree workbook, and reading a hand-authored one.
openpyxl==3.1.5

# Reconstructing a workbook from photographs (tools/build_spec_xlsx.py only).
# Not needed to run a crawl or a verification.
pypdf==5.1.0
pillow==11.0.0
