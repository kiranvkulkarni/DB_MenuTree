# Pipeline
requests==2.31.0
python-dotenv==1.0.0
pyyaml==6.0.1
pydantic==2.5.3

# MenuTree workbook export
openpyxl==3.1.5

# Manifest parsing -- supplies the activity-coverage denominator
androguard==4.1.2

# DroidBot's own runtime deps (installed transitively, pinned here for clarity)
networkx==3.2.1
opencv-python==4.9.0.80

# DroidBot itself is not on PyPI in a usable form; install from source:
#   pip install git+https://github.com/honeynet/droidbot.git
# Do NOT use MobileLLM/AutoDroid: its installed entrypoint drops the -policy
# flag and hardcodes the task-directed LLM policy, which cannot do exhaustive
# coverage. See README section on crawler choice.
